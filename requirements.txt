# This file is not needed for the project.
# It is only used to install the dependencies for the project.
# The dependencies are already installed in the environment.

PyQt6>=6.0.0
mysql-connector-python>=8.0.0
python-dotenv>=0.19.0